% Load the dictionary file
dictionary = readtable('dictionary.txt');

% Create a bag-of-words model
bag = bagOfWords(tokenizedText, dictionary);

% Split the data into training and test sets
partition = cvpartition(labels, 'Holdout', 0.3);
trainingData = bag(partition.training, :);
testData = bag(partition.test, :);

% Train the classifier
classifier = fitcnb(trainingData, labels(partition.training));

% Test the classifier
predictions = predict(classifier, testData);
